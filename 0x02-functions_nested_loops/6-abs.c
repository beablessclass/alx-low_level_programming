#include "main.h"

/**
 * _abs - Computes the absolute value of a whole number
 * @n: The integer to be checked.
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
